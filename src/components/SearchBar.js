import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SearchBar = () => {
	return (
		<View>
			<Text>Buscar en el</Text>
		</View>
	);
};

export default SearchBar;

const styles = StyleSheet.create({});
